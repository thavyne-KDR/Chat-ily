# Chatily App

Aplicação Expo + Node (tRPC) — template/cliente-servidor para prototipagem.

Descrição rápida

- Frontend em Expo / React Native + `expo-router`.
- Backend leve em TypeScript (pasta `server`) usado para APIs e serviços locais.

# Chatily App

Aplicação Expo + Node (tRPC).

Comandos rápidos:

```bash
pnpm install
pnpm dev
```
